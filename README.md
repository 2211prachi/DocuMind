# 🧠 DocuMind

**AI-powered document analysis & question answering system** built with a RAG (Retrieval-Augmented Generation) pipeline.

Upload PDF documents, and ask natural-language questions — DocuMind retrieves the most relevant passages and generates accurate, context-grounded answers.

---

## ✨ Features

| Feature | Description |
|---|---|
| **PDF Ingestion** | Upload PDFs; pages are extracted, chunked, and embedded into a vector store |
| **Semantic Search** | Queries are matched against document chunks using cosine similarity |
| **AI Answers** | Answers are generated by Flan-T5 using only the retrieved context |
| **Query Rewriting** | An agent rewrites user queries for better retrieval (GPT-2) |
| **Tool Routing** | Simple router dispatches time/date queries to a tool node |
| **Answer Evaluation** | A lightweight eval node triggers re-generation for low-quality answers |
| **Chat Memory** | Last 5 Q&A pairs are kept in memory for conversational context |
| **Streamlit UI** | Beautiful chat-style frontend with PDF upload sidebar |

---

## 🏗️ Architecture

```
User ──▶ Streamlit UI ──▶ FastAPI Backend
                              │
                    ┌─────────┴──────────┐
                    ▼                    ▼
              Tool Router          Query Rewriter (GPT-2)
                    │                    │
                    ▼                    ▼
              Tool Node           Retrieval (ChromaDB)
              (time/date)               │
                    │                    ▼
                    │            Generation (Flan-T5)
                    │                    │
                    ▼                    ▼
                    └────────┬───────────┘
                             ▼
                       Eval Node
                             │
                             ▼
                       Chat Memory
                             │
                             ▼
                        Response
```

---

## 🛠️ Tech Stack

- **Backend**: [FastAPI](https://fastapi.tiangolo.com/)
- **Frontend**: [Streamlit](https://streamlit.io/)
- **Embeddings**: `sentence-transformers/all-MiniLM-L6-v2` via HuggingFace
- **Generation**: `google/flan-t5-base` (text2text-generation)
- **Query Agent**: `gpt2` (text-generation)
- **Vector Store**: [ChromaDB](https://www.trychroma.com/)
- **PDF Parsing**: LangChain `PyPDFLoader`
- **Chunking**: LangChain `RecursiveCharacterTextSplitter`

---

## 📁 Project Structure

```
backend/
├── main.py                 # FastAPI app — /upload & /query endpoints
├── streamlit_app.py        # Streamlit frontend
├── requirements.txt        # Python dependencies
├── services/
│   ├── ingestion.py        # PDF loading, chunking, embedding
│   ├── retrieval.py        # Similarity search against ChromaDB
│   ├── generation.py       # Answer generation with Flan-T5
│   ├── agent.py            # Query rewriting with GPT-2
│   └── graph.py            # Pipeline state graph (alternative runner)
├── uploaded_files/         # Uploaded PDFs stored here
├── chroma_db/              # Persisted ChromaDB vector store
├── answer_query.py         # Standalone CLI query script
├── store_vectors.py        # Standalone CLI ingestion script
├── chunk_test.py           # Test: PDF chunking
├── query_test.py           # Test: vector retrieval
└── test_pdf.py             # Test: PDF loading
```

---

## 🚀 Getting Started

### Prerequisites

- Python 3.9+
- pip

### 1. Clone the repository

```bash
git clone https://github.com/2211prachi/DocuMind.git
cd DocuMind/backend
```

### 2. Create a virtual environment

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# macOS / Linux
source venv/bin/activate
```

### 3. Install dependencies

```bash
pip install -r requirements.txt
```

### 4. Run the FastAPI backend

```bash
uvicorn main:app --reload
```

The API will be available at `http://localhost:8000`.

### 5. Run the Streamlit frontend

Open a **second terminal** (with the venv activated):

```bash
streamlit run streamlit_app.py
```

The UI will open at `http://localhost:8501`.

---

## 📡 API Reference

### `POST /upload`

Upload and process a PDF file.

**Request**: `multipart/form-data` with a `file` field (PDF only).

**Response**:
```json
{
  "message": "PDF processed successfully",
  "filename": "sample.pdf",
  "pages": 12,
  "chunks": 45
}
```

---

### `POST /query`

Ask a question about your uploaded documents.

**Request**:
```json
{
  "question": "What is the main topic of the document?",
  "filename": "sample.pdf"
}
```

**Response**:
```json
{
  "answer": "The document discusses...",
  "sources": [{"page": 3, "file": "sample.pdf"}],
  "explanation": ["Retrieved from page 3 of sample.pdf"],
  "confidence": 0.8,
  "metrics": {
    "retrieval_time": 0.42,
    "chunks_used": 3
  }
}
```

---

## 📝 Usage

1. **Upload a PDF** — Use the sidebar in the Streamlit UI or send a `POST /upload` request.
2. **Ask questions** — Type your question in the chat input. Optionally filter by filename.
3. **Review sources** — Expand the "View Sources" section to see which pages were used.

---

## 📄 License

This project is open-source. Feel free to use and modify it.
